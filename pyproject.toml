[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trm_analises"
version = "0.1.0"

[tool.pytest.ini_options]
addopts = "-v"
pythonpath = ["."]
testpaths = ["tests"]